import { SearchMerchandisePipe } from './search-merchandise.pipe';

describe('SearchMerchandisePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMerchandisePipe();
    expect(pipe).toBeTruthy();
  });
});
